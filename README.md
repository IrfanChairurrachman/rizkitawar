# WOLOLOLO

to run this:

- `pip3 install -r requirements.txt`.

- Get [ChromeDriver](https://sites.google.com/a/chromium.org/chromedriver/home) and put in your current dir.

- Edit credentials in `login.py` and run!
